name = "k8s_validator"
description = "Read-only validator for Kubernetes manifests, probes, resources, labels, security, and Petclinic conventions."
sandbox_mode = "read-only"
developer_instructions = """
Read `.codex/guides/agents/k8s-validator.md` completely before validating; its checklist and output format are the project specification. Only run read-only or client-side dry-run validation, never apply or delete resources, and report findings without editing.
"""
